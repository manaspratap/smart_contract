name: smart_contract
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.26.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  web3dart: ^2.3.5
  http: ^0.13.4
  cupertino_icons: ^1.0.0
  blockfrost: ^1.0.0-dev.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - src/abis/Investment.json
